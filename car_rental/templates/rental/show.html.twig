{% extends 'base.html.twig' %}

{% block title %}Rental{% endblock %}

{% block body %}
<div class="container">

    <h1 class="display-2 text-center">Rental</h1>
    


    <div class="row">
        <div class="col-lg-6">
            <img class="img-thumbnail" src="{{ rental.img }}" alt="">
        </div>
        <div class="col-lg-6">
            <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ rental.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ rental.description }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ rental.year }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ rental.price }}</td>
            </tr>
        </tbody>
    </table>
        </div>
    </div>

    <a href="{{ path('rental_index') }}" class="btn btn-info">back to list</a>

    <a href="{{ path('rental_edit', {'id': rental.id}) }}" class="btn btn-primary">edit</a>

    {{ include('rental/_delete_form.html.twig') }}
    
</div>
{% endblock %}
